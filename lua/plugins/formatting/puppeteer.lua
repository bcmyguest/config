-- https://github.com/chrisgrieser/nvim-puppeteer
-- converts strings into f-strings

return { {
	"chrisgrieser/nvim-puppeteer",
	lazy = false, -- plugin lazy-loads itself. Can also load on filetypes.
}, }
