---
# uv installs into ~/.local/bin (on PATH via XDG default). The role also runs
# `uv tool update-shell` so any `uv tool install` executables land in that dir
# and are callable directly (no `uvx` prefix needed).
